## Para minikube !!

# apiVersion: v1
# kind: Service
# metadata:
#   name: app-vote-service
# spec:
#   type: NodePort
#   selector:
#     app: app-vote
#   ports:
#     - port: 80
#       targetPort: 80

## Para EKS !!
apiVersion: v1
kind: Service
metadata:
  name: app-vote-service
spec:
  type: LoadBalancer
  selector:
    app: app-vote
  ports:
    - port: 80
