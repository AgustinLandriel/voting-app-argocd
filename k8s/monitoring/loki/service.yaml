apiVersion: v1
kind: Service
metadata:
  name: loki
  namespace: monitoring
spec:
  selector:
    app: loki
  ports:
    ## Un solo puerto para todo: por aca Promtail ESCRIBE (push)
    ## y Grafana LEE (query).
    - name: http
      port: 3100
      targetPort: http
